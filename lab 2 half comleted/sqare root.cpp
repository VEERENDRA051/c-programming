/*this programe is used to find the sqare root of number*/
#include<stdio.h>
#include<math.h>
int main( )
{
	float a,b;
	printf("enter the number req to find the sqare root");
	scanf("%f",&a);
	b=sqrt(a);
	printf("the sqare root of the num is %f",b);
	return 0;
}
